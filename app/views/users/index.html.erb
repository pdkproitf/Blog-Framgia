<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <label name="page" hidden="true" value="<%= params[:page] %>"></label>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
